# AutoClaw Context Pack — Sprint 1 — other

_Generated 2026-07-06T12:07:45.576Z. Read this before you start: it grounds you in this project's real code, proven patterns, learned style, recent memory, and durable facts. These are retrieved hints, not authority — verify against the current code before relying on them._

**Tasks:** next-cline

## Grounded Context (RAG-retrieved)

**Task:** Next available work for cline: Auto-dispatched by orchestrator-loop. Agent cline is idle.

## Relevant Code from Your Project

_Code retrieval was unavailable (vector backend degraded); this prompt was built from learnings, style, and memory only._

## Your Previously Successful Patterns

- Kept (git commit): {"AllowMultiple":false,"Description":"Initialize scratchpad with task list","EndLine":1,"Instruction":"Initialize scratchpad with task list","ReplacementContent":"# Scratchpad - Debugging FDB Booklet
- Kept (git commit): {"ClickType":"left","PageID":"AC5AFC5562021D145B429AB0ABCABCA7","X":635,"Y":371,"explanation":"Click the Next Page button in the virtual booklet viewer.","toolAction":"Clicking next page","toolSummary
- Kept (git commit): +┌──────────────────────────────────────────────────────────────────┐
- Kept (git commit): {"JavaScriptDescription":"Click the Vendor Tracker nav item","JavaScriptSource":"(() =\u003e {\n const items = Array.from(document.querySelectorAll('[class*=\"navItem\"]'));\n const vt = items.find(

## Your Learned Agent Style Guide

## Agent Style Guide

_Generated by AutoClaw `/learn`. This file is regenerated on every run from your recent sessions — edit the source signals, not this file._

## Successful Patterns

- Kept (git commit): {"AllowMultiple":false,"Description":"Initialize scratchpad with task list","EndLine":1,"Instruction":"Initialize scratchpad with task list","ReplacementContent":"# Scratchpad - Debugging FDB Booklet
- Kept (git commit): {"ClickType":"left","PageID":"AC5AFC5562021D145B429AB0ABCABCA7","X":635,"Y":371,"explanation":"Click the Next Page button in the virtual booklet viewer.","toolAction":"Clicking next page","toolSummary
- Kept (git commit): +┌──────────────────────────────────────────────────────────────────┐
- Kept (git commit): {"JavaScriptDescription":"Click the Vendor Tracker nav item","JavaScriptSource":"(() =\u003e {\n const items = Array.from(document.querySelectorAll('[class*=\"navItem\"]'));\n const vt = items.find(
- Kept (git commit): {"JavaScriptDescription":"Get list of options in the crop selector dropdown.","JavaScriptSource":"(() =\u003e {\n const select = document.querySelector('select');\n if (!select) return 'No select fo
- Kept (git commit): const trimmed = text.trim().toUpperCase();
- Kept (git commit): // Fetch membership_tier and ai_autopsies_used
- Kept (git commit): const result = await runPolicyPipeline();
- Kept (git commit): Apply this to the `summary` before insert.

## Patterns to Avoid

- Avoid large speculative rewrites that are not backed by tests.
- Avoid adding new dependencies when an existing project utility already covers the need.

## Preferred Tools

- Gemini CLI
- Cline / Roo

## Project Memory Summary (recent)

ng FDB Booklet
- Kept (git commit): {"ClickType":"left","PageID":"AC5AFC5562021D145B429AB0ABCABCA7","X":635,"Y":371,"explanation":"Click the Next Page button in the virtual booklet viewer.","toolAction":"Clicking next page","toolSummary
- Kept (git commit): +┌──────────────────────────────────────────────────────────────────┐
- Kept (git commit): {"JavaScriptDescription":"Click the Vendor Tracker nav item","JavaScriptSource":"(() =\u003e {\n const items = Array.from(document.querySelectorAll('[class*=\"navItem\"]'));\n const vt = items.find(
- Kept (git commit): {"JavaScriptDescription":"Get list of options in the crop selector dropdown.","JavaScriptSource":"(() =\u003e {\n const select = document.querySelector('select');\n if (!select) return 'No select fo
- Kept (git commit): const trimmed = text.trim().toUpperCase();
- Kept (git commit): // Fetch membership_tier and ai_autopsies_used
- Kept (git commit): const result = await runPolicyPipeline();
- Kept (git commit): Apply this to the `summary` before insert.
Patterns to avoid:
- Avoid large speculative rewrites that are not backed by tests.
- Avoid adding new dependencies when an existing project utility already covers the need.

## Instructions for the Agent

- Follow the patterns and style shown in the retrieved examples above.
- Prefer solutions that match what has been successfully kept in past sessions.
- Avoid approaches recorded under patterns to avoid.
- Match existing project conventions; read code before changing it.
- Be concise and engineering-focused; produce production-ready code.
- If the task is ambiguous, ask clarifying questions before coding.

## Durable Knowledge-Graph Facts

- *(observation)* Dispatched Next available work for cline (next-cline) to other for sprint 1.
- *(observation)* Dispatched Next available work for cline (next-cline) to other for sprint 1.
- *(observation)* Dispatched Next available work for cline (next-cline) to other for sprint 1.
- *(observation)* Dispatched Next available work for cline (next-cline) to other for sprint 1.
- *(observation)* Dispatched Next available work for cline (next-cline) to other for sprint 1.
- *(observation)* Dispatched Next available work for cline (next-cline) to other for sprint 1.
